# Simple Node Practice

*   to run a string use

    ```bash
        Node -e "console.log('test')"
    ```
*   to debug use Node debug <file name>. Type "next" to change the debug point. **debug** is deprecated instead use **inspect**.